This project has been migrated to GIT and renamed as sisu-inject:

  http://github.com/sonatype/sisu/tree/master/sisu-inject/

The main artifacts are now:

  <groupId>org.sonatype.sisu</groupId>
  <artifactId>guice-patches</artifactId>

  <groupId>org.sonatype.sisu</groupId>
  <artifactId>sisu-inject-bean</artifactId>

  <groupId>org.sonatype.sisu</groupId>
  <artifactId>sisu-inject-plexus</artifactId>

SVN history has been preserved, but no more changes will be picked up.
